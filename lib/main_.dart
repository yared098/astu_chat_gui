// ignore_for_file: prefer_const_constructors

import 'package:flutter/material.dart';

class main_dart extends StatelessWidget {
  const main_dart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("hdjhskjdhkjas")),
    );
  }
}
